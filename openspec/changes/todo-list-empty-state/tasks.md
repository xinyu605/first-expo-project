# Tasks: Todo List Empty State

## 1. 空狀態元件與 FlatList

- [x] 1.1 在 `TodoListScreen` 內新增空狀態 UI（例如 `ListEmptyComponent` 用的函式或元件），顯示「尚無待辦」及簡短說明。
- [x] 1.2 空狀態樣式使用 `useTheme()` 的 `colors` 與 theme 的 `fontSizes`、`spacing`，不寫死色碼或 px。
- [x] 1.3 將空狀態元件傳入 `FlatList` 的 `ListEmptyComponent` prop。

## 2. 驗證

- [x] 2.1 手動測試：清空所有待辦後，確認列表區顯示空狀態且樣式正確（含深/淺色主題）。
